# Next Implementation Steps

1. Replace in-memory AppState with EF Core DbContext + PostgreSQL entities
2. Add JWT Bearer authentication + refresh token storage
3. Add SignalR authenticated user groups + realtime events
4. Add file upload endpoint + media storage
5. Add NgRx effects and full store slices
6. Add Dexie offline drafts + background sync queue
7. Add comments/reactions/notifications if desired
